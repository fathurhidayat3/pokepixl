import styled from "styled-components";

const ContentWrapper = styled.div`
  flex: 1;

  margin: 0 auto;
  padding: 16px;

  width: calc(100% - 32px);
  max-width: 480px;
`;

export default ContentWrapper;
